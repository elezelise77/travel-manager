# 변경 기록

## 2.2.0

- **데스크톱 앱에서 주소 관리** — 여행방마다 웹 앱 주소와 구글시트 주소를 확인·수정할 수 있습니다
  - 새 서버는 시트 주소와 Apps Script 편집기 주소를 알려주므로 **자동으로 채워집니다** (✓ 자동 확인됨)
  - 예전 서버는 시트 주소를 직접 붙여넣으면 됩니다
  - [시트 열기] · [코드 편집기 열기] · [연결 확인] 버튼, 툴바에 [시트]
  - 업데이트 안내의 첫 단계가 **편집기 바로 열기** 버튼으로 바뀌었습니다
- **여행방 목록이 날아가지 않게** — 임시 파일에 쓰고 바꿔치기, 직전본을 `.bak` 으로 보관.
  앱을 다시 설치해도 `%APPDATA%\trip-board` 에 그대로 남습니다
- 웹앱 설정 탭(관리자)에 [시트 열기] · [코드 편집기] 버튼

## 2.1.0

- **화면 자동 업데이트** — 화면(Index.html)을 시트에 붙박아 두지 않고 깃허브 Pages 에서 받아옵니다.
  화면을 고치면 쓰는 사람들이 붙여넣지 않아도 반영됩니다.
  - 받아온 화면은 6시간 보관 (매 접속마다 받아오지 않습니다)
  - 못 받아오면 **시트에 붙여넣어 둔 화면으로 자동 폴백** — 주소가 죽어도 앱은 멈추지 않습니다
  - 받아온 내용이 이 앱의 화면인지 검사한 뒤에만 씁니다
  - 설정 탭에서 켬/끔, [지금 확인] 으로 즉시 반영
  - 설정 탭 버전 줄에 출처 표시 (최신 / 최신(보관중) / 시트 / 시트(가져오기 실패))
  - 켜는 법은 `자동업데이트-켜는법.txt` 참고
- **범용 창구 `api(token, action, args)`** — google.script.run 이 Code.gs 의 함수만 부를 수 있는 제약 때문에
  기능을 추가할 때마다 Code.gs 를 고쳐야 했습니다. list / put / del / ensure / getcfg / setcfg 를 열어두어
  앞으로 새 탭 정도는 화면만 고쳐도 만들 수 있습니다.
  기본 시트는 구조 변경 불가, 비밀번호·권한 열은 읽기·쓰기 모두 차단합니다.
- 서버 로직(Code.gs)은 의도적으로 자동화하지 않았습니다. 구매자 시트를 직접 읽고 쓰는 코드라
  원격 실행은 위험합니다. 대신 EXE 가 버전을 비교해 안내합니다.

## 2.0.0

설치 방식이 바뀌었습니다. 템플릿 시트를 복사하는 대신, **본인 계정에 빈 스프레드시트를 만들어**
코드를 붙여넣습니다. 남의 시트에 들어 있던 자료가 딸려오지 않습니다.

- **설치 마법사 재작성** — ① 빈 시트 만들기 → ② 코드 두 개 붙여넣기 → ③ 배포 → ④ 주소 등록
  - `Code.gs` / `Index.html` 을 **버튼 한 번으로 클립보드에 복사**해 줍니다
  - 템플릿 주소(`ui/config.js`) 는 더 이상 쓰지 않습니다
- **업데이트 안내** — 앱에 들어 있는 코드 버전과 시트에 올라간 버전을 비교해
  다르면 띠로 알리고, 붙여넣는 순서와 `배포 관리 → 새 버전` 까지 화면으로 안내합니다
- **부분 참여 정산** — 지출 항목마다 나눌 사람을 고를 수 있습니다 (비우면 참석자 전원).
  1원 단위까지 배분해 부담금 합계 = 총지출, 잔액 합계 = 0 이 항상 맞습니다
- **백업 되돌리기** — 내려받아 둔 백업 JSON 으로 전부 복구 (비밀번호는 지금 것 유지)
- **백업 내려받기** — 웹앱 설정 탭에서도 가능 (예전에는 EXE 에서만)
- **다음 여행 준비** — 명단·계좌·비밀번호는 남기고 지출·일정·숙소·건의사항만 비우기
- **내 자료 전부 비우기** — 시트 메뉴. 시트를 남에게 넘기기 전에 쓰는 기능
- 시트 `지출대장` 에 `참여자` 열이 자동으로 추가됩니다 (기존 자료는 그대로)

## 1.5.0

- **정산 인원을 참석 여부로 계산합니다** (가장 큰 변경)
  - '불참'인 사람은 N분의 1에서 언제나 빠집니다
  - '미정'·무응답은 설정에서 [포함 / 제외] 선택 (기본: 포함)
  - 인원이 바뀌면 1인당 금액이 다시 잡히고, **이미 보낸 돈은 그대로 두고
    모자란 만큼만 더 보내거나 남는 만큼 돌려받도록** 자동 재계산됩니다
- 참석 여부를 바꾸기 전에 "정산 인원 5명 → 4명, 1인당 75,000원 → 100,000원"을 먼저 보여주고 확인받습니다
- 미정 인원이 있으면 정산 탭에 "아직 잠정 금액입니다" 안내 + 모두 불참했을 때의 금액을 미리 보여줍니다
- 사람별 현황에 [불참] · [미정] 표시. 불참자가 이미 결제·송금한 돈은 전액 돌려받도록 계산됩니다
- 설정 > 관리자에 [정산 인원] 항목 추가
- 모바일에서 사람별 현황이 표와 카드로 두 번 나오던 것 수정

## 1.4.1

- 데스크톱 앱: 앱 안쪽 화면이 안 열릴 때 흰 화면으로 멈추지 않고 안내 화면이 나옵니다
  (새 창으로 열기 / 기본 브라우저로 열기 / 다시 시도)
- 데스크톱 앱: 툴바에 [새 창] 추가 — 앱 안쪽 화면 제약을 받지 않는 별도 창
- 데스크톱 앱: 툴바에 [점검] 추가 — 앱 버전, 서버 버전, 연결 상태를 한눈에 확인
- 데스크톱 앱: [새로고침]이 캐시까지 비워서 옛 화면이 남지 않습니다
- 웹앱: 로그인 화면과 설정 탭에 버전 표시 (화면 / 서버)
- 웹앱: 설정 항목이 바뀌어도 시트 점검이 다시 돌도록 수정

## 1.4.0 — 2026-09-14
- **지출 조기 마감** (관리자) — 더 이상 지출이 없을 때 잠가서 금액을 확정
  - 마감하면 관리자 포함 아무도 지출을 추가·수정·삭제할 수 없음 (설정에서 해제 가능)
  - 지출대장·대시보드에 마감 시각과 안내 표시
  - **여행후일괄 모드라도 지출이 마감되면 송금 기록이 열림**
- 여행후일괄 모드일 때 일반 멤버에게 안내 강화
  - 로그인 직후 알림 한 번 + 대시보드 상단 배너 + 정산 탭 문구
  - "여행이 끝난 뒤 한 번에 정산합니다. 송금은 여행 끝나고 나서 해주세요!"

## 1.3.0 — 2026-09-14
- **정산 방식을 납부 기록 방식으로 재설계**
  - 예전: "이 송금 끝남" 체크 하나만 저장 → 지출이 바뀌면 숫자가 꼬임
  - 이제: 실제로 오간 돈을 한 건씩 쌓고 잔액은 항상 다시 계산
  - 잔액 = (내가 결제) − (내 부담금) + (내가 보냄) − (내가 받음)
  - **선납** · **부분 납부** · **환급**이 모두 같은 방식으로 처리됨
- 지출을 수정하면 과납한 사람에게 **돌려줄 금액이 자동으로 계산**됨
- 관리자용 **정산 방식** 설정 추가 (수시정산 / 여행후일괄)
- 정산 탭에 사람별 현황표(부담금·결제·보냄·받음·잔액)와 주고받은 내역 추가
- 데스크톱 앱: 템플릿 시트 주소를 `ui/config.js` 로 분리 (저장소에 올라가지 않음)

## 1.2.0 — 2026-09-14
- 사진 직접 업로드 (구글 드라이브) · 올리기 전 미리보기 · 갤러리 크게 보기와 좌우 넘기기
- 시트 항목이 바뀌면 머리글을 자동으로 한 번 더 점검 (schemaSig)
- 지출대장 모바일 카드 레이아웃
- 앱 아이콘 추가

## 1.1.0 — 2026-09-14
- 움직이는 설치 가이드(SETUP-GUIDE.html)
- 기본 비밀번호를 관리자가 설정
- 모바일 최적화 (탭 줄바꿈, 바텀시트 입력창, 터치 영역 확대)

## 1.0.0 — 2026-09-13
- 첫 버전 — 이름+비밀번호 로그인, N분의 1 정산, 일정 · 식사 · 이동 · 숙소 · 참석자 · 사진 · 건의사항
